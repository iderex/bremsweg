//! The neighbour of `unformatted.rs`. It says the same thing and is formatted,
//! which is the only difference between the two, so a formatter that refused
//! both would prove nothing.

pub fn stopping_power(energy: f64) -> f64 {
    energy
}
